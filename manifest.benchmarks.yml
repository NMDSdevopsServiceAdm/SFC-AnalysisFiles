applications:
  - name: sfcreports-benchmark
    memory: 8G
    disk_quota: 4GB
    instances: 1
    buildpack: https://github.com/cloudfoundry/nodejs-buildpack.git#v1.7.31
    services:
      - sfc-db-benchmark
    env:
      SERVICE_NAME: sfc-db-benchmark
      NODE_ENV: benchmarks
stack: cflinuxfs3